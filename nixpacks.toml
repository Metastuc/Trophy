[phases.setup]
nixPkgs = ["nodePackages_latest.nodejs", "bun", "openssl_3", "pkg-config"]
# cmds = [
#     'cp /usr/lib/x86_64-linux-gnu/libcrypto.so.3 /usr/lib/x86_64-linux-gnu/libssl.so.3 /app/',
# ]
