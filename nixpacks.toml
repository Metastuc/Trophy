[phases.setup]
nixPkgs = ["nodePackages_latest.nodejs", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["cp .env.local .env", "pnpm run build"]

[start]
cmds = ["cp .env.local .env", "pnpm exec prisma db push", "pnpm run prod"]
