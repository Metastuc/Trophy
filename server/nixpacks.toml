[phases.setup]
nixPkgs = [
  "nodePackages_latest.nodejs",
  "yarn"
]

[environment]
PATH = "/app/node_modules/.bin:$PATH"

[phases.install]
cmds = [
  "yarn install --frozen-lockfile",
  "yarn migrate"
]

[start]
cmd = "yarn start"
