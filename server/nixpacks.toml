[phases.setup]
nixPkgs = ["bun", "nodejs_20", "openssl", "pkg-config", "bash", "coreutils"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = [
  "bunx prisma generate",
  "bunx prisma migrate deploy"
]

[phases.start]
cmd = "bun run server.ts"
